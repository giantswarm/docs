kind: Service
apiVersion: v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: content
  labels:
    app: {{ .Values.name }}
    component: content
spec:
  ports:
    - port: 80
      targetPort: http
  selector:
    app: {{ .Values.name }}
    component: content
