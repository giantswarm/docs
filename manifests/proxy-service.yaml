kind: Service
apiVersion: v1
metadata:
  namespace: docs
  name: proxy
  labels:
    app: docs
    component: proxy
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
  selector:
    app: docs
    component: proxy
