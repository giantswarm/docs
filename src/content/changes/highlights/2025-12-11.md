---
date: 2025-12-11T14:00:00
title: Highlights for the week ending 2025-12-11
changes_categories:
  - Highlights
owner:
  - https://github.com/orgs/giantswarm/teams/sig-product
---

## General

<!-- This where BREAKING CHANGES ARE HIGHLIGHTED -->

## Apps

- [grafana-app](https://github.com/giantswarm/grafana-app) 
  - [2.28.5](https://github.com/giantswarm/grafana-app/compare/v2.28.4...v2.28.5)
      * upgrade `pgClusterRecoveryTest` image tag: 17.5 => 18.1
  - [2.28.4](https://github.com/giantswarm/grafana-app/compare/v2.28.3...v2.28.4)
      * upgrade giantswarm/postgresql-cnpg image tag: 17.7 => 18.1 


  - [2.28.3](https://github.com/giantswarm/grafana-app/compare/v2.28.2...v2.28.3)
      * Set `postgresqlCluster.enabled` to `false` to fix the ATS ci job. 


- [external-dns-app](https://github.com/giantswarm/external-dns-app) 
  - [3.3.0](https://github.com/giantswarm/external-dns-
app/compare/v3.2.0...v3.3.0) 
### Important changes
      * TXT registry record formatting has changed in this version. After upgrading, external-dns creates new TXT records, but existing (old) records will not be deleted automatically; manual cleanup by the operator is required.
      * The value `.Values.provider` has been deprecated in favour of `.Values.provider.name`.
      * Use kubectl-apply-job when installing CRDs.
      * Upgrade external-dns to v0.20.0.
      * Update DNSEndpoints CRD.
      * Sync to upstream helm chart `1.19.0`.
      * Grant `discovery.k8s.io/endpointslices` permission only when using `service` source. 
      * Update RBAC for `Service` source to support `EndpointSlices`.
      * Allow extraArgs to also be a map enabling overrides of individual values.
      * Set defaults for `automountServiceAccountToken` and `serviceAccount.automountServiceAccountToken` to `true` in Helm chart values.
      * Correctly handle `txtPrefix` and `txtSuffix` arguments when both are provided.
      * Add ability to generate schema with `helm plugin schema`.
      * Regenerate JSON schema with `helm-values-schema-json' plugin.
      * Added ability to configure `imagePullSecrets` via helm `global` value.
      * Added options to configure `labelFilter` and `managedRecordTypes` via dedicated helm values.
      * Allow templating `serviceaccount.annotations` keys and values, by rendering them using the `tpl` built-in function.
      * Added support for `extraContainers` argument.
      * Added support for setting `excludeDomains` argument.
      * Added support for setting `dnsConfig`.
      * Added support for webhook providers.
      * Restrict managed record types to A and CNAME.
- [backstage](https://github.com/giantswarm/backstage) 
  - [0.91.0](https://github.com/giantswarm/backstage/compare/v0.90.2...v0.91.0) 
      * `ChartPicker` custom scaffolder field.
      * `ChartTagPicker` custom scaffolder field.
      * `YamlValuesEditor` custom scaffolder field.
      * `YamlValuesValidation` custom scaffolder field.
      * GS backend plugin.
      * Container Registry frontend API client and backend service.
      * `helmcharts` catalog entity annotation format has been changed.
See
[./docs/releases/v0.91.0-changelog.md](./docs/releases/v0.91.0-changelog.md)
for more information.
- [hello-world-app](https://github.com/giantswarm/hello-world-app) 
  - [2.10.0](https://github.com/giantswarm/hello-world-
app/compare/v2.9.1...v2.10.0) 
      * Chart: add annotations `io.giantswarm.application.audience` and `io.giantswarm.application.managed` to metadata.
      * Chart: change annotation `application.giantswarm.io/team` to the OCI-compliant form `io.giantswarm.application.team`.
      * Chart: update `sources` list.
      * Chart: update `keywords` list.
      * Chart: Remvove `maintainers` key.
      * Template helpers: Remove `giantswarm.io/service-type: managed` annotation.
- [konfigure](https://github.com/giantswarm/konfigure) 
  - [2.1.1](https://github.com/giantswarm/konfigure/compare/v2.1.0...v2.1.1) 
      * Improve info and error message for variable loading in `render` function.
- [cluster-aws](https://github.com/giantswarm/cluster-aws) 
  - [7.1.0](https://github.com/giantswarm/cluster-aws/compare/v7.0.0...v7.1.0) 
      * Chart: Update `cluster` to v5.1.0.
  - [7.0.0](https://github.com/giantswarm/cluster-aws/compare/v6.4.0...v7.0.0) 
### ⚠️ Breaking changes
      * The following IAM permissions have been removed from the control plane nodes
        * autoscaling:SetDesiredCapacity
        * autoscaling:TerminateInstanceInAutoScalingGroup
      * Removed `global.providerSpecific.reducedInstanceProfileIamPermissionsForWorkers` value, as that's the default behavior now. It cannot be overridden anymore.
      * _This change will roll the nodes_ Add Crossplane IAM Roles, policies and instance profiles for worker and control plane nodes. Instead of having an IAM Role per node pool, now we'll use the same for all node pools.
      * Add the `priority-classes` default app, enabled by default. This app provides standardised `PriorityClass` resources like `giantswarm-critical` and `giantswarm-high`, which should replace the previous inconsistent per-app priority classes.
      * _This change will roll the nodes on Karpenter node pools_ Attach the `lb` Security Group to Karpenter nodes.
      * _This change will roll the nodes on Karpenter node pools_ Name instance on AWS after the nodepool name.
      * Chart: Update `cluster` to v5.0.0.
      * Reduce redundant parts of JSON schema for Karpenter vs. MachinePool types of node pools
      * Adjust node max pods based on the `nodeCidrMaskSize`
      * Add missing documentation for node pools (health checks were not listed)
      * Ensure defaulting `maxHealthyPercentage` since Helm does not use the default from the schema 


- [cluster-azure](https://github.com/giantswarm/cluster-azure) 
  - [5.1.0](https://github.com/giantswarm/cluster-azure/compare/v5.0.0...v5.1.0)
      * Chart: Update `cluster` to v5.1.0.
  - [5.0.0](https://github.com/giantswarm/cluster-azure/compare/v4.4.0...v5.0.0)
      * Add the `priority-classes` default app, enabled by default. This app provides standardised `PriorityClass` resources like `giantswarm-critical` and `giantswarm-high`, which should replace the previous inconsistent per-app priority classes.
      * Add `"helm.sh/resource-policy": keep` annotation to `AzureCluster` CR so that it doesn't get removed by Helm when uninstalling this chart. The CAPI controllers will take care of removing it, following the expected deletion order.
      * Chart: Update `cluster` to v5.0.0. 


- [valkey-app](https://github.com/giantswarm/valkey-app) 
  - [0.1.2](https://github.com/giantswarm/valkey-app/compare/v0.1.1...v0.1.2) 
      * Add resource requests/limits for init container to pass kube-linter checks.
      * Add resource requests/limits for metrics exporter container.
      * Add securityContext with readOnlyRootFilesystem for metrics exporter container.
  - [0.1.1](https://github.com/giantswarm/valkey-app/compare/v0.1.0...v0.1.1) 
      * Add icon URL to Chart.yaml to fix app-build-suite validation. 


  - [0.1.0](https://github.com/giantswarm/valkey-app/releases/tag/v0.1.0) 
      * Initial release based on upstream valkey-helm chart v0.8.1.
      * Configure Giant Swarm container registry for images.
      * Add CiliumNetworkPolicy for network isolation.
      * Add VerticalPodAutoscaler (VPA) support.
      * Enable metrics and PodMonitor for Prometheus monitoring. 


- [cluster-api-ipam-provider-in-cluster-app](https://github.com/giantswarm/cluster-api-ipam-provider-in-cluster-app) 
  - [0.1.3](https://github.com/giantswarm/cluster-api-ipam-provider-in-cluster-
app/compare/v0.1.2...v0.1.3) 
      * Add `node-role.kubernetes.io/control-plane` toleration.
- [cluster-api-provider-vsphere-app](https://github.com/giantswarm/cluster-api-provider-vsphere-app) 
  - [2.0.1](https://github.com/giantswarm/cluster-api-provider-vsphere-
app/compare/v2.0.0...v2.0.1) 
      * Temporarily disable pushing to CAPA addons collection.
      * Correct tag for container image.
  - [2.0.0](https://github.com/giantswarm/cluster-api-provider-vsphere-
app/compare/v1.0.0...v2.0.0) 
      * Chart: Update CAPV to v1.14.0. 


- [cluster-cloud-director](https://github.com/giantswarm/cluster-cloud-director) 
  - [3.1.0](https://github.com/giantswarm/cluster-cloud-
director/compare/v3.0.0...v3.1.0) 
      * Chart: Update `cluster` to v5.1.0.
- [cluster-vsphere](https://github.com/giantswarm/cluster-vsphere) 
  - [4.1.0](https://github.com/giantswarm/cluster-vsphere/compare/v4.0.0...v4.1.0)
      * Chart: Update `cluster` to v5.1.0.
- [dex-app](https://github.com/giantswarm/dex-app) 
  - [2.1.4](https://github.com/giantswarm/dex-app/compare/v2.1.3...v2.1.4) 
      * Auto-include mcpKubernetes in dex-k8s-authenticator trustedPeers for seamless token exchange.
  - [2.1.3](https://github.com/giantswarm/dex-app/compare/v2.1.2...v2.1.3) 
      * Add PodLogs for log collection.
      * Fix support for using a private CA in the ingresses 


- [gel-app](https://github.com/giantswarm/gel-app) 
  - [1.0.1](https://github.com/giantswarm/gel-app/compare/v1.0.0...v1.0.1) 
      * Disable VPA by default due to issues during schema migrations.
      * Increase CPU and memory requests.
      * Allow inter-pod networking for PostgreSQL databse instances.
- [kyverno-app](https://github.com/giantswarm/kyverno-app) 
  - [0.22.0](https://github.com/giantswarm/kyverno-app/compare/v0.21.0...v0.22.0)
      * Update `kyverno` to upstream version v1.16.1.
### Notes
This release includes an upstream update. Please refer to the following
Release Notes from upstream for the latest changes:
      * [v1.16.0](https://github.com/kyverno/kyverno/releases/tag/v1.16.0)
      * [v1.16.1](https://github.com/kyverno/kyverno/releases/tag/v1.16.1)
- [cluster](https://github.com/giantswarm/cluster) 
  - [5.1.0](https://github.com/giantswarm/cluster/compare/v5.0.0...v5.1.0) 
      * Add `insecure` flag to containerd mirrors to configure them as http instead of hard coding https.
      * Add `debug.level` flag to containerd mirrors to configure containerd logging verbosity.
      * Add `overridePath` flag to containerd mirrors to configure containerd `override_path` flag.
      * Add support for Kubernetes Structured Authentication Configuration for OIDC providers. This feature allows configuring multiple OIDC issuers and is supported on **Kubernetes 1.34+**. The feature is disabled by default and can be enabled via `global.controlPlane.oidc.structuredAuthentication.enabled`. When enabled, the API server uses `AuthenticationConfiguration` instead of legacy `--oidc-*` flags. **Note:** Existing OIDC configuration (legacy flags) is automatically migrated and configured as the first issuer in the new structure, ensuring a seamless transition. This feature also adds support for custom CEL expressions for claim mappings and claim validation rules.
      * Chart: Update sandbox image to v3.10. ([#731](https://github.com/giantswarm/cluster/pull/731))
- [gpu-operator-app](https://github.com/giantswarm/gpu-operator-app) 
  - [1.1.1](https://github.com/giantswarm/gpu-operator-
app/compare/v1.1.0...v1.1.1) 
      * Chart: Update `gpu-operator` to v25.10.1. ([#29](https://github.com/giantswarm/gpu-operator-app/pull/29))

## Docs

<!-- FER is filling this one -->
